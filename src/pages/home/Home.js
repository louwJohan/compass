import React from "react";
import Slider from "../../components/Slider";

// Renders home page with carousel
const Home = () => {
  return (
    <div>
      <Slider />
    </div>
  );
};

export default Home;
